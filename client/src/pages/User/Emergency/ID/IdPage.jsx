/**
 * ID Page — Emergency mode Profile
 * 
 * Profile is the SAME in both modes, so we re-export from General.
 * This is the ONE exception to the "no cross-imports" rule.
 */
export { default } from "@/pages/User/General/Profile/ProfilePage";
